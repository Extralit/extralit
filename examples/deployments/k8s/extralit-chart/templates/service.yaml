apiVersion: v1
kind: Service
metadata:
  name: { { include "extralit.fullname" . } }
  labels: { { - include "extralit.labels" . | nindent 4 } }
spec:
  selector: { { - include "extralit.selectorLabels" . | nindent 4 } }
  ports:
    - name: http
      port: 6900
