// This spec file was committed empty in #200, which fails Jest with
// "Your test suite must contain at least one test" and broke the frontend
// CI test step (npm run test:coverage). The placeholder below keeps the
// suite green until real unit tests for the view model are written.
describe("useImportFileUploadViewModel", () => {
  it.todo("add unit tests for the import file-upload view model");
});
